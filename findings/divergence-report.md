# Divergence Report — spec-v8 vs harness-* codebase

> Generated during Phase 0. **Must be reviewed and accepted before Phase 2+ code changes land.**
> Column key: *spec-says* = what spec-v8 prescribes | *code-says* = what the code actually does | *proposed-amendment* = how to reconcile | *authority-tier* = USER-BINDING / STRONG DEFAULT / PROVISIONAL

| Rule / Pillar | spec-says | code-says | proposed-amendment | authority-tier |
|---|---|---|---|---|
| **Rule W** (single-writer) | Each file written by exactly one agent at a time | DAG conflict guard already enforces this in both `harness-plan:854-872` and `harness-implement:388-407`; parallel tasks that share a file are downgraded to sequential | Already satisfied. Extract to `lib/dag.js` for test coverage. No behavior change. | PROVISIONAL (premise already false — do not rebuild) |
| **Rule M** (mode switching) | Spec claimed ad-hoc model switching is the problem; prescribed explicit ROUTING table | Models are already allocated by phase at top of each skill, not scattered ad-hoc. Cost tables are duplicated but the allocation logic is sound. | Already satisfied at the design level. Centralize into `lib/models.js` + `lib/routing.js` for maintenance. | PROVISIONAL (spec premise false — allocation already principled) |
| **T1.2 / T1.4** (agent death / null handling) | Skills must handle agent death gracefully | Workflow runtime already returns `null` on agent death. All three skills filter `.filter(Boolean)` on parallel results. | Already satisfied. Runtime provides the guarantee; do not rebuild it. | STRONG DEFAULT (runtime contract — do not rebuild) |
| **T1.5** (watchdog / timeout) | Skills should implement a watchdog for hung agents | The Workflow runtime uses an `await`-model — there is no hook to inject a watchdog timer around an in-flight `agent()` call. You cannot set a per-call timeout in workflow scripts. | **AMEND spec.** T1.5 is infeasible in the Workflow await-model. Mitigation: rely on the runtime's built-in concurrency cap and total-agent cap (1000). Document this limit in each SKILL.md. | PROVISIONAL (infeasible — AMEND) |
| **H-B** (multi-writer) | Skills may run in parallel and need a concurrency coordinator | All three skills are invoked as separate top-level Workflow runs — they never share a workspace concurrently. There is no multi-writer scenario. | Already satisfied. No coordinator needed. | PROVISIONAL (premise false for this deployment model) |
| **Stale model ID** (harness-plan) | Use current model IDs | `harness-plan/workflow.js:99` has `opusModel = 'anthropic.claude-opus-4-6-v1'` — this ID no longer exists. Correct ID is `claude-opus-4-8`. | Fix in Phase 2: replace with `MODEL.opus` from `lib/models.js`. | STRONG DEFAULT (bug — must fix) |
| **Effort parameter** | Agents should declare effort tier | No `effort` param on any agent call in any skill (currently unset = defaults to Sonnet-level reasoning for all tiers including Haiku I/O). | Fix in Phase 3: Haiku read/IO agents → `effort: 'low'`; synthesis → `'medium'`; verifier/architect → `'high'`. Encoded in `lib/routing.js`. | STRONG DEFAULT (efficiency improvement) |
| **Import resolution** | `workflow.js` should import helpers from `./lib/` | The Workflow runtime does **not** support `import()` in workflow scripts (`import() is not available in workflow scripts` — verified by probe in Phase 0.0). | Fallback applied: pure functions live in `lib/` as authoritative source (tests import from there). When wiring in Phase 1+, a `// ===== PURE (mirrors lib/) =====` delimited block is added to the top of each `workflow.js`. Tests import from `lib/` and lock both copies. | USER-BINDING (probe result — do not change without re-probing) |
