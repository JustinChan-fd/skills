---
name: harness-split
description: DEPRECATED — use harness-intake instead. Forwards to harness-intake workflow.
---

# harness-split (deprecated alias)

**DO NOT use this skill. Run `/harness-intake` instead.**

harness-intake supersedes harness-split entirely. If you are here because the user typed `/harness-split`, stop and tell them:

> `/harness-split` is deprecated. Please use `/harness-intake <jira-url>` instead — it handles all sizes (XS/S/M/L) and does everything harness-split did plus AC inference and intake manifests.

Do not invoke any workflow. Do not run any shell commands. Just redirect the user to `/harness-intake`.
