.DS_Store
**/.DS_Store
*.swp

# Node
node_modules/

# Hook runtime artifacts. skill-observability writes its records OUTSIDE the repo
# (~/.claude/skill-runs by default, SKILL_OBS_DIR to override), so these two rules
# only catch stray local scratch — there is no in-repo log store to protect.
*.log
*.state
