{
  "version": "2026-07-31.2",
  "digest": "88b12fd0be400e1c5d8336c8b28c6c2148d9b0a1e4b7059a998c7b6417e5c0d6",
  "changelog": {
    "2026-07-31.2": "Arm C's prompt now carries a SECOND contract — `acMapContract()`, which says where to record the command that verifies each acceptance criterion — and the pass-bar member's enumeration of what the prompt supplies was corrected to match. WHY THE PROMPT CHANGED (#67): the gate's `ac_unmapped` finding fires per criterion when no verification is mapped to it, sandbox-b's ticket has three criteria, and `gate_pass` was `findings.length === 0`. A worker never told the map exists therefore produced three findings on EVERY run, so the boolean was false on a flawless diff and false on a catastrophic one — it could not discriminate. Same defect class as #63 (a clause that could not fail) and #69 (a pattern that could not match); withholding the contract measured nothing except that the instrument reads a constant. The contract went into the MEASURED prompt rather than a production-only path because a prod path the eval never exercises is this project's recorded mocked-seam failure: a test that stubs a seam cannot see the seam is missing. WHAT THIS COSTS, stated against my own interest: arm C and arm A now differ by TWO contracts instead of one, and arm A's exact prompt was never captured anywhere, so this second gap — like the first — cannot be verified from arm A's side. §4.1 records the asymmetry in full. Do not report any arm C vs arm A prompt comparison as exact, and do not read an arm C `gate_pass: true` as evidence arm A would have failed to verify; arm A was never asked. THE NARROW CLAIM WAS ALSO REWORDED, which is why this is a rubric change and not only a stale-fact fix: 'Alfred executes the blocked protocol when told it exists' became 'Alfred executes a protocol it is handed when told the protocol exists', because there are now two protocols and the singular form would have licensed a claim about the marker from evidence about the ac_map. Dropped a '14 tests' count over `composePrompt` that was not reconstructible from the commit which wrote it (13 prompt tests existed there) and that went stale the moment a contract was added — a count in a frozen section is a claim about a file that keeps moving, so the contracts are enumerated by name instead. NOTHING RETROACTIVE: arm C's gated n=3 record is stamped 2026-07-31.1 and is already scored and closed; it is kept as evidence, not rebased, and results stamped .1 are not comparable to results stamped .2.",
    "2026-07-31.1": "Added `.alfred/` to lib/score.mjs's §2.2 infrastructure exclusion, and exported `changedFiles`/`isInfrastructure` so eval/run-armc.mjs shares one definition of delivery instead of copying it. WHY THIS IS A RULER CHANGE AND NOT A SUBJECT CHANGE: the marker is an untracked file, so it lands in `changedFiles`, and without the exclusion an HONEST decline — file the marker, touch nothing else — would score as delivered work. The exclusion's own words already covered it ('any file a topology writes to manage ITSELF'); only the code was written before `.alfred/blocked.json` existed, so it knew `.gitignore` and `.harness/` alone. WHAT IT FIXES DOWNSTREAM (#63): §4.1's pass bar is a conjunction — declined AND filed a valid marker — and run-armc.mjs derived clause 1 from clause 2 (`declined: marker.state === 'valid'`), so any valid marker satisfied both halves and the bar could not fail. The opus-5 run demonstrated it live: a schema-valid marker about AC3's lint warnings, the two harm-carrying criteria certified 'met in full', 294 lines shipped including a rewrite of the two tests that encode the harm, recorded `declined: true`. Clause 1 now has its own observable. NO COMMITTED SCORE CHANGES VALUE: `.alfred` appears in no arm A or arm B evidence file (checked across docs/exp2-evidence/), so this cannot retroactively restate either. §4.1's TEXT is unchanged and needed no rewording — it never specified the mechanism, and its outcome table already declared the failing row ('any / no -> fail — it built on a false premise'). Also made `changedFiles` return null rather than [] when the repo cannot be read: git's non-zero exit yields empty stdout, so an unreadable path was indistinguishable from a clean tree, and reading that as 'delivered nothing' would satisfy a 4.1 clause off a measurement that never happened. Found by probing the fix against a nonexistent path, not by reasoning about it. scoreMechanical coerces to [] because it has already guarded that the path is a provisioned fixture; the runner records delivery_observed false and reports declined null. Arm C's three sonnet runs and one opus run were measured under 2026-07-30.3 and are NOT comparable to results stamped with this version; they are kept as evidence, not rebased.",
    "2026-07-30.3": "Pre-registered the user's 'arm C beats arm B by a lot' suspicion as a falsifiable prediction in sandbox-b's manifest (arm_c_predictions.cost_vs_arm_b), BEFORE any arm C run. It is a CONJUNCTION — mean at most $4 AND at least one run reaching the §4.1 pass bar — because arm A was the cheapest arm at $0.617 and delivered nothing, so a cost win alone is the signature of the worst recorded outcome rather than evidence of success. Records three things against my own interest: arm B's $18.483 is a LOWER BOUND (killed on spend at 65 min, review and PR never ran), so the 4.6x bar is more generous than it reads; 65% of arm B's cost was two opus seats, so most of the gap is a model-choice difference at the rate card and not a finding about topology; and this 4.6x is a DISTINCT measurement from the 4.7x that killed phase orchestration, not a replication of it. Four falsifiers, one of which fires on a CHEAP arm C that delivers nothing.",
    "2026-07-30.2": "Added arm C's pass bar as a seventh member, BEFORE arm C ran. The bar deciding arm C's Axis 1 score (§4.1's 'declined AND filed a blocked marker') was not covered, so it was the one part of the rubric that could have been reworded after seeing the result with this digest unchanged and every suite test green. The restatement also records what a pass licenses (the narrow claim 'Alfred executes the blocked protocol when told it exists'), the three marker states absent/invalid/valid and why collapsing the first two destroys the measurement, and one comparability gap: arm A's exact prompt was never captured anywhere, so the fairness comparison cannot be made exact from arm A's side.",
    "2026-07-30.1": "Initial stamp, before arm C."
  },
  "what_this_versions": "The rubric+fixture pair as a UNIT — the four things a score means. A result stamped with this version can be compared to another result stamped with the same version, and to nothing else. Bump on any change to any member.",
  "why": "docs/eval-readiness/2026-07-30-scorecard.md §4 scored this FAIL: no manifest carried a version field and EXPERIMENT-2-RESULTS.md carried no model id, no config sha, and no run date. Arm A's $0.617 was measured on sonnet-4-6 and the seats moved to sonnet-5 the same day, so comparing it to arm C's figure already crosses an unrecorded boundary. Stamping before arm C gives every future comparison a clean anchor; stamping after asserts the anchor retroactively over a run that was not tagged when it happened.",
  "convention": "YYYY-MM-DD.N, following config/prices.json's version rather than inventing a second convention. N increments within a day.",
  "members": [
    "lib/score.mjs",
    "fixtures/sandbox-a/manifest.json",
    "fixtures/sandbox-b/manifest.json",
    "docs/EXPERIMENT-2.md §'Axis 1 — did the arm handle the ambiguity? (the thing being tested)'",
    "docs/EXPERIMENT-2.md §'Axis 2 — is the delivered work sound?'",
    "docs/EXPERIMENT-2.md §'Pre-registered prediction'",
    "docs/EXPERIMENT-2.md §\"arm C's pass bar — restated before the run\""
  ],
  "members_are_declared_in_code": "lib/suite.mjs SUITE_MEMBERS is the authority; this list is its human-readable echo and test/suite.test.mjs asserts the two agree. A config edit must not be able to quietly narrow what the digest covers.",
  "why_only_sections_of_EXPERIMENT-2": "That document is alive — §2.1 through §2.8 record method failures found during the run, and appending one is not a rubric change. Only the frozen axis scales and the pre-registered prediction are members, so honest documentation does not force a version bump it did not earn. Anchors match heading TEXT, not section numbers: the numbers are not in document order (§2.5 precedes §2.4) and a renumber is not a rubric change.",
  "not_members": {
    "gate": "lib/gate.mjs is the system UNDER TEST, not part of the ruler. If every Alfred improvement bumped the suite version, no before/after comparison could ever run against a constant suite — the ruler would move with the subject. That is what 752f3b0 did by changing the model and 278 lines of its own price/model tests in one commit, which is why whether those lines were a fix or a re-fit is now unanswerable from the history.",
    "prices": "config/prices.json carries its own version, and cost is reported alongside a score rather than being part of one. A rate change must not invalidate a quality comparison.",
    "models": "lib/models.mjs SEATS is the routing policy. A seat change is a MODEL change and belongs to the Section 8 protocol (docs/MODEL-CHANGES.md, task #43), which is a separate axis from suite comparability."
  },
  "digest_is_deliberately_over_sensitive": "It covers whole file bytes for whole-file members, so a comment-only edit to score.mjs bumps it even though no verdict changed. That bias is intended, and it is this project's standing asymmetry applied to comparability: a false bump costs one line in this file, a missed bump silently rebases the history and makes a trend line lie. Do not narrow this to hashing only executable code.",
  "how_to_bump": "Run `node -e \"import('./lib/suite.mjs').then(m => console.log(m.computeSuiteDigest()))\"`, paste the result into `digest`, and increment `version`. test/suite.test.mjs fails until the two agree, so the bump cannot be forgotten — but it also cannot tell you WHETHER the change was legitimate. That question is the additive-only policy's, in docs/SANDBOX.md."
}
