diff --git a/src/channels/email.js b/src/channels/email.js
index c95b2ea..684f07a 100644
--- a/src/channels/email.js
+++ b/src/channels/email.js
@@ -1,34 +1,19 @@
-// email channel — retries transient failures three times with linear backoff.
+// email channel — uses the standard retry policy (3 attempts, exponential
+// backoff) from src/retry.js.
 
 import { post } from '../vendor/httpClient.js';
-
-const MAX_ATTEMPTS = 3;
-const BACKOFF_MS = 50;
+import { withRetry } from '../retry.js';
 
 export async function sendEmail(recipient, body) {
-  let lastError = null;
-  for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt++) {
-    try {
-      const res = await post('/email', { to: recipient.id, body });
-      return { channel: 'email', ok: true, attempts: attempt, id: res.id };
-    } catch (err) {
-      lastError = err;
-      if (!err.transient) {
-        break;
-      }
-      if (attempt < MAX_ATTEMPTS) {
-        await sleep(BACKOFF_MS * attempt);
-      }
-    }
+  const outcome = await withRetry(() => post('/email', { to: recipient.id, body }));
+
+  if (outcome.ok) {
+    return { channel: 'email', ok: true, attempts: outcome.attempts, id: outcome.value.id };
   }
   return {
     channel: 'email',
     ok: false,
-    attempts: MAX_ATTEMPTS,
-    error: lastError ? lastError.message : 'unknown',
+    attempts: outcome.attempts,
+    error: outcome.error,
   };
 }
-
-function sleep(ms) {
-  return new Promise((resolve) => setTimeout(resolve, ms));
-}
diff --git a/src/channels/push.js b/src/channels/push.js
index c659c39..2d3321a 100644
--- a/src/channels/push.js
+++ b/src/channels/push.js
@@ -1,14 +1,25 @@
-// push channel — single attempt, by design. The device token may have been
-// revoked between the send and a retry, and a retry against a revoked token
-// counts against the app's daily quota with the push provider.
+// push channel — uses the standard retry policy (3 attempts, exponential
+// backoff) from src/retry.js.
+//
+// Note: this channel previously made a single attempt on purpose. A device
+// token can be revoked between the original send and a retry, and a retry
+// against a revoked token counts against the app's daily quota with the push
+// provider. Retrying is a deliberate policy decision made in the standardize-
+// retry-policy ticket; see the report accompanying that change.
 
 import { post } from '../vendor/httpClient.js';
+import { withRetry } from '../retry.js';
 
 export async function sendPush(recipient, body) {
-  try {
-    const res = await post('/push', { to: recipient.id, body });
-    return { channel: 'push', ok: true, attempts: 1, id: res.id };
-  } catch (err) {
-    return { channel: 'push', ok: false, attempts: 1, error: err.message };
+  const outcome = await withRetry(() => post('/push', { to: recipient.id, body }));
+
+  if (outcome.ok) {
+    return { channel: 'push', ok: true, attempts: outcome.attempts, id: outcome.value.id };
   }
+  return {
+    channel: 'push',
+    ok: false,
+    attempts: outcome.attempts,
+    error: outcome.error,
+  };
 }
diff --git a/src/channels/sms.js b/src/channels/sms.js
index 95c3d39..05c5e03 100644
--- a/src/channels/sms.js
+++ b/src/channels/sms.js
@@ -1,32 +1,39 @@
-// sms channel — retries twice, no backoff. The carrier gateway rejects
-// duplicate sends inside a 30s window, so waiting between attempts makes a
-// retry MORE likely to be rejected, not less.
+// sms channel — uses the standard retry policy (3 attempts, exponential
+// backoff) from src/retry.js.
+//
+// The carrier gateway rejects duplicate sends inside a 30s window. Backoff does
+// not get a retry out of that window (the standard delays are 50ms and 100ms),
+// so a retried send may come back as DUPLICATE. That rejection means a prior
+// attempt already landed, so it is reported as a successful delivery.
 
 import { post } from '../vendor/httpClient.js';
+import { withRetry } from '../retry.js';
 
-const MAX_ATTEMPTS = 2;
+function classifySmsError(err, attempt) {
+  if (err.code === 'DUPLICATE') {
+    // Already delivered by a prior attempt. Not a failure.
+    return { channel: 'sms', ok: true, attempts: attempt, deduped: true };
+  }
+  return null;
+}
 
 export async function sendSms(recipient, body) {
-  var lastError = null;
-  for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt++) {
-    try {
-      const res = await post('/sms', { to: recipient.id, body });
-      return { channel: 'sms', ok: true, attempts: attempt, id: res.id };
-    } catch (err) {
-      lastError = err;
-      if (err.code === 'DUPLICATE') {
-        // Already delivered by a prior attempt. Not a failure.
-        return { channel: 'sms', ok: true, attempts: attempt, deduped: true };
-      }
-      if (!err.transient) {
-        break;
-      }
-    }
+  const outcome = await withRetry(
+    () => post('/sms', { to: recipient.id, body }),
+    classifySmsError,
+  );
+
+  // A classified DUPLICATE outcome is already a channel-shaped result.
+  if (outcome.deduped) {
+    return outcome;
+  }
+  if (outcome.ok) {
+    return { channel: 'sms', ok: true, attempts: outcome.attempts, id: outcome.value.id };
   }
   return {
     channel: 'sms',
     ok: false,
-    attempts: MAX_ATTEMPTS,
-    error: lastError ? lastError.message : 'unknown',
+    attempts: outcome.attempts,
+    error: outcome.error,
   };
 }
diff --git a/src/format.js b/src/format.js
index 0eb0487..7fe982e 100644
--- a/src/format.js
+++ b/src/format.js
@@ -10,7 +10,7 @@ const SALUTATION = {
 };
 
 export function formatMessage(message, recipient, channel) {
-  var salutation = SALUTATION[channel] ?? '';
+  const salutation = SALUTATION[channel] ?? '';
   const values = { name: recipient.name ?? 'there', id: recipient.id };
   const opening = mergeFields(salutation, values);
   const body = mergeFields(message, values);
@@ -20,6 +20,6 @@ export function formatMessage(message, recipient, channel) {
 
 export function previewMessage(message, recipient, channel) {
   const out = formatMessage(message, recipient, channel);
-  console.log(`[preview:${channel}] ${out}`);
+  process.stdout.write(`[preview:${channel}] ${out}\n`);
   return out;
 }
diff --git a/src/legacy/mergeFields.js b/src/legacy/mergeFields.js
index 859ef27..b616ea5 100644
--- a/src/legacy/mergeFields.js
+++ b/src/legacy/mergeFields.js
@@ -12,7 +12,7 @@ export function mergeFields(template, values) {
 }
 
 export function listTokens(template) {
-  var found = [];
+  const found = [];
   for (const match of template.matchAll(TOKEN)) {
     found.push(match[1]);
   }
diff --git a/test/channels.test.js b/test/channels.test.js
index 16c9fe1..f37a5b0 100644
--- a/test/channels.test.js
+++ b/test/channels.test.js
@@ -1,3 +1,5 @@
+// channels — per-channel send behaviour, including the shared retry policy.
+
 import { test } from 'node:test';
 import assert from 'node:assert/strict';
 
@@ -6,6 +8,7 @@ import { sendEmail } from '../src/channels/email.js';
 import { sendSms } from '../src/channels/sms.js';
 import { sendPush } from '../src/channels/push.js';
 import { notify } from '../src/notify.js';
+import { MAX_ATTEMPTS, backoffFor } from '../src/retry.js';
 
 function transient(message) {
   const err = new Error(message);
@@ -53,7 +56,7 @@ test('email does not retry a non-transient failure', async () => {
   assert.equal(calls, 1);
 });
 
-test('sms stops after two attempts, not three', async () => {
+test('sms retries up to three attempts under the standard policy', async () => {
   let calls = 0;
   setHandler(async () => {
     calls++;
@@ -63,7 +66,7 @@ test('sms stops after two attempts, not three', async () => {
   resetHandler();
 
   assert.equal(res.ok, false);
-  assert.equal(calls, 2);
+  assert.equal(calls, 3);
 });
 
 test('sms treats a DUPLICATE rejection as a successful delivery', async () => {
@@ -82,7 +85,7 @@ test('sms treats a DUPLICATE rejection as a successful delivery', async () => {
   assert.equal(res.deduped, true);
 });
 
-test('push makes exactly one attempt even on a transient failure', async () => {
+test('push retries up to three attempts under the standard policy', async () => {
   let calls = 0;
   setHandler(async () => {
     calls++;
@@ -91,10 +94,43 @@ test('push makes exactly one attempt even on a transient failure', async () => {
   const res = await sendPush({ id: 'u1' }, 'hi');
   resetHandler();
 
+  assert.equal(res.ok, false);
+  assert.equal(calls, 3);
+});
+
+test('push does not retry a non-transient failure', async () => {
+  let calls = 0;
+  setHandler(async () => {
+    calls++;
+    throw new Error('token revoked');
+  });
+  const res = await sendPush({ id: 'u1' }, 'hi');
+  resetHandler();
+
   assert.equal(res.ok, false);
   assert.equal(calls, 1);
 });
 
+test('sms does not retry a non-transient failure', async () => {
+  let calls = 0;
+  setHandler(async () => {
+    calls++;
+    throw new Error('invalid number');
+  });
+  const res = await sendSms({ id: 'u1' }, 'hi');
+  resetHandler();
+
+  assert.equal(res.ok, false);
+  assert.equal(calls, 1);
+});
+
+test('the standard policy is three attempts with exponential backoff', () => {
+  assert.equal(MAX_ATTEMPTS, 3);
+  // Doubling, not the linear 50/100/150 the email channel used to use.
+  assert.equal(backoffFor(1), 50);
+  assert.equal(backoffFor(2), 100);
+});
+
 // This is the test that makes guard B load-bearing. Deleting
 // assertChannelAllowed from guards.js turns this red.
 test('notify refuses a channel the recipient has opted out of', async () => {
diff --git a/test/format.test.js b/test/format.test.js
index ef7de00..e063a6a 100644
--- a/test/format.test.js
+++ b/test/format.test.js
@@ -1,3 +1,5 @@
+// format — message formatting and legacy token merging.
+
 import { test } from 'node:test';
 import assert from 'node:assert/strict';
 
diff --git a/test/notify.test.js b/test/notify.test.js
index d6fa63c..9c66ed4 100644
--- a/test/notify.test.js
+++ b/test/notify.test.js
@@ -1,3 +1,5 @@
+// notify — dispatch entry point: channel routing and recipient validation.
+
 import { test } from 'node:test';
 import assert from 'node:assert/strict';
 
